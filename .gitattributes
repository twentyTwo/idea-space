# The is script runs under bash, which rejects CRLF line endings. Keep it LF on
# every checkout, Windows included, so an installed plugin never ships a broken script.
plugins/idea-space/scripts/is text eol=lf
