# The ms script runs under bash, which rejects CRLF line endings. Keep it LF on
# every checkout, Windows included, so an installed plugin never ships a broken script.
plugins/ms/scripts/ms text eol=lf
